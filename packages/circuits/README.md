# @loud/circuits

Circom circuits for Open Loud's PLONK-based privacy core.

## Circuits

- `withdraw.circom` — proves knowledge of a note secret without revealing it
- `nullifier.circom` — prevents double-spending

## Build

```bash
pnpm build
